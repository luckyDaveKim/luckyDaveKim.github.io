# Jekyll configuration
markdown: kramdown
highlighter: rouge
paginate: 11 # it will paginate after this number
language: 'ko-kr'
words_per_minute: 200

# edit baseurl to simply '/' if using as your personal page (instead of a project page)
baseurl: /

# Website info
title: DevDave
description: 프로그래밍으로 예술을 꿈꾸다
cover: assets/images/blog-cover.jpg
logo: assets/images/blog-icon.png
logo_dark: assets/images/favicon.png
favicon: assets/images/favicon.png
CNAME: #add cname for website to be created

# Social info
navigation: true # show the navbar links
subscribers: false # you need to connect an external service for this to work,
# if 'true' the submit button is disabled for now, but you can change that
# by editing `_includes/subscribe-form.html`
# twitter: # replace by your username
facebook: lucky.dave.k # replace by your username

# Disqus
disqus: true
disqus_shortname: lucky-dave # replace with your disqus username

# Google Analytics
google_analytics: UA-144176044-1 # replace with your GA tracking identifier

# Permalinks
permalink: /:categories/:title
# permalink: /author/:author
# permalink: /tag/:tag

# Authors' info in `_data/authors.yml`
# Tags' info in `_data/tags_info.yml`

# gems and other configs
plugins_dir: [_plugins]
plugins: [jekyll-paginate, jekyll-feed]

feed:
  path: feed.xml

# Additional settings available on the front-matter
# Site logo in the index.html
# Author's page cover in _layouts/author.html
# The same for page.html and tag.html layouts
# Post's page cover as a per-post basis _posts/...
# Disqus comments as a per-post basis
# ...

# Settings for builing master branch with travis-ci
# with jekyll-travis

# Settings for deploy rake task
# Username and repo of Github repo, e.g.
# https://github.com/USERNAME/REPO.git
# username defaults to ENV['GIT_NAME'] used by Travis
# repo defaults to USERNAME.github.io
# Branch defaults to "source" for USERNAME.github.io
# or "master" otherwise
safe: false
lsi: false
username: luckyDaveKim
repo: luckyDaveKim.github.io
branch: gh-core
relative_source:
destination: ../pages/
production_url: https://luckydavekim.github.io/
source_url: https://github.com/luckyDaveKim/luckyDaveKim.github.io/

exclude:
  - assets/css
  - node_modules
  - vendor
  - .travis.yml
  - Gemfile
  - Gemfile.lock
  - gulpfile.js
  - LICENSE
  - package.json
  - Rakefile
  - README.md
  - script.py
  - changelog.md
  - "*.Rmd"
  - .git*
